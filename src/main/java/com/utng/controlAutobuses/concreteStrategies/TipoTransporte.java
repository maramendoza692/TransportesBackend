package com.utng.controlAutobuses.concreteStrategies;

import java.util.Arrays;

public enum TipoTransporte {
	
	    AVION, AUTOBUS, UBER;
	    
	    /*public static boolean exists(TipoAutobus type) {
	    	return Arrays.stream(TipoAutobus.values().anyMatch(type::equals));
	    }*/
	
}
